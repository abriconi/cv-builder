export interface ExperienceItemType {
  id: string;
  title: string;
  companyName: string;
  startDate: string;
  endDate: string;
  location: string;
  description: string;
}

export interface SelectOptions {
  value: string;
  label: string;
}
